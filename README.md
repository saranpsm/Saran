# Saran

Program-1.java => It handle the exception also if the user not enter the numbers it take the input continues if the non of the operator is give as the input in the second time it exits from the calculations from the calculatore.

Program-2.java => It get the input how many iteration is need after it iterared in the loop for the pattern j increment j+=2 for the give pattern to print.
 and the time complexity is O(n) and space complexity is O(1)

program-3.java => same as the program-2.java if only check the even number is divide it reduce the number with 1 as n=n-1 by using this it able to print it by the given pattern  and the time complexity is O(n) and space complexity is O(1)

program-4.java => It used the number is divisible it calculated the increment the count in the count array by using the inp[j]%arr[i]==0 it check the condition if it is true it increment and the time complexity is O(n2) and space complexity is O(n)
